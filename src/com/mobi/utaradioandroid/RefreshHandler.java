package com.mobi.utaradioandroid;

import android.app.Activity;
import android.os.Bundle;
import android.util.Log;
import android.view.View;

public class RefreshHandler extends Activity {
	/*
	static final String URL = "http://radio.uta.edu/mediaplayer/nowplaying.xml";

	@Override
	protected void onCreate(Bundle savedInstanceState) {
		// TODO Auto-generated method stub
		super.onCreate(savedInstanceState);

		new Thread(new Runnable() {
			public void run() {
				int sleepTimeInSeconds = 3;
				try {
					Thread.sleep(sleepTimeInSeconds * 1000);
					XmlParser xp = new XmlParser(URL);
					Log.d("USER", xp.getInfo());

					Thread.sleep(500);
					run();
				} catch (InterruptedException e) {
					// TODO Auto-generated catch block
					e.printStackTrace();
				}
			}
		}).start();

	}
	*/
}
